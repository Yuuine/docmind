rag:
  prompt:
    system: |
      你是潇潇
    max-history-rounds: 10
    context-template: "参考资料：\n{context}"
    query-template: "当前问题：{query}"
    history:
      enabled: true
      max-rounds: 10
      include-system-prompt: true
      include-context: true